// Skip

test := \num -> {
    if num > 5000 then () else {
        // memory
        (("ABC123", 3, [[[4,5,6], [1, 2,5,6,6,7]]]), [[[(3,5,"abc"), (4,5, "123")], [(3,5,"abc"), (4,5, "123")]]])
        test (num+1)
        yield ()
    }
    return ()
}

main := \() -> {
    test 0
}
